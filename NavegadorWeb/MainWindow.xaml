<Window x:Class="NavegadorWeb.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NavegadorWeb"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Aurora Browser" Height="768" Width="1024"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        SourceInitialized="MainWindow_SourceInitialized"
        StateChanged="Window_StateChanged">

    <Window.Resources>
        <Color x:Key="DefaultBrowserBackgroundColor">#282c34</Color> <SolidColorBrush x:Key="DefaultBrowserBackgroundBrush" Color="{StaticResource DefaultBrowserBackgroundColor}"/>

        <Color x:Key="DefaultBrowserForegroundColor">#abb2bf</Color> <SolidColorBrush x:Key="DefaultBrowserForegroundBrush" Color="{StaticResource DefaultBrowserForegroundColor}"/>

        <Color x:Key="GeminiBackgroundColor">#343a40</Color> <SolidColorBrush x:Key="GeminiBackgroundBrush" Color="{StaticResource GeminiBackgroundColor}"/>

        <Color x:Key="GeminiControlBackgroundColor">#3a4049</Color> <SolidColorBrush x:Key="GeminiControlBackgroundBrush" Color="{StaticResource GeminiControlBackgroundColor}"/>

        <Color x:Key="GeminiAccentColor">#007bff</Color> <SolidColorBrush x:Key="GeminiAccentBrush" Color="{StaticResource GeminiAccentColor}"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource GeminiControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DefaultBrowserForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#454c57"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2f343a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource GeminiControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DefaultBrowserForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#454c57"/>
            <Setter Property="Padding" Value="5"/>  <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TabHeaderTemplate" TargetType="TabItem">
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding IconSource}" Width="16" Height="16" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Title}" VerticalAlignment="Center" Foreground="{StaticResource DefaultBrowserForegroundBrush}"/>
                            <Button Grid.Column="2" Content="x" Command="{Binding CloseTabCommand}"
                                    Width="16" Height="16"
                                    Margin="5,0,0,0"
                                    Background="Transparent"
                                    Foreground="{StaticResource DefaultBrowserForegroundColor}"
                                    BorderThickness="0"
                                    ToolTip="Close tab"
                                    VerticalAlignment="Center">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#FF6060"/> </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#E55050"/> </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TabItemStyle" TargetType="{x:Type TabItem}">
            <Setter Property="Background" Value="{StaticResource GeminiBackgroundColor}"/>
            <Setter Property="Foreground" Value="{StaticResource DefaultBrowserForegroundColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,1,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="4,4,0,0"
                                Margin="0">
                            <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="{TemplateBinding Padding}"
                                        RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DefaultBrowserBackgroundColor}"/>
                                <Setter Property="Panel.ZIndex" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4a505b"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource GeminiBackgroundColor}"/>
            <Setter Property="Foreground" Value="{StaticResource DefaultBrowserForegroundColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#454c57"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource DefaultBrowserForegroundColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="Transparent" BorderThickness="0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter ContentSource="Header" Margin="5"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource GeminiAccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder"
            Background="{StaticResource DefaultBrowserBackgroundBrush}"
            CornerRadius="8"
            BorderThickness="1"
            BorderBrush="{StaticResource GeminiControlBackgroundBrush}"
            MouseLeftButtonDown="MainBorder_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>    <RowDefinition Height="Auto"/>    <RowDefinition Height="Auto"/>    <RowDefinition Height="*"/>       <RowDefinition Height="Auto"/>    </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Row="0" Grid.Column="0"
                  x:Name="TitleBarGrid"
                  Background="{StaticResource DefaultBrowserBackgroundBrush}"
                  Height="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Image x:Name="WindowIcon" Grid.Column="0" Source="Icons/app.ico" Width="20" Height="20" Margin="5,0"/>

                <TextBlock Text="{Binding ElementName=BrowserTabs, Path=SelectedItem.Title}"
                           Grid.Column="1"
                           VerticalAlignment="Center" HorizontalAlignment="Left"
                           Margin="10,0"
                           Foreground="{StaticResource DefaultBrowserForegroundColor}"
                           MouseLeftButtonDown="TitleBarGrid_MouseLeftButtonDown"/>

                <Button x:Name="MinimizeButton" Content="—" Grid.Column="3" Click="MinimizeButton_Click" ToolTip="Minimize"/>
                <Button x:Name="MaximizeRestoreButton" Content="⬜" Grid.Column="4" Click="MaximizeRestoreButton_Click" ToolTip="Maximize"/>
                <Button x:Name="CloseButton" Content="✕" Grid.Column="5" Click="CloseButton_Click" ToolTip="Close"/>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="0" Margin="5,0,5,5" Height="35">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="*"/>    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Click="BackButton_Click" ToolTip="Back" Margin="0,0,5,0">
                    <Image Source="/NavegadorWeb;component/Icons/back.png" Width="20" Height="20"/>
                </Button>
                <Button Grid.Column="1" Click="ForwardButton_Click" ToolTip="Forward" Margin="0,0,5,0">
                    <Image Source="/NavegadorWeb;component/Icons/forward.png" Width="20" Height="20"/>
                </Button>
                <Button Grid.Column="2" Click="RefreshButton_Click" ToolTip="Refresh" Margin="0,0,5,0">
                    <Image Source="/NavegadorWeb;component/Icons/refresh.png" Width="20" Height="20"/>
                </Button>
                <Button Grid.Column="3" Click="HomeButton_Click" ToolTip="Home" Margin="0,0,5,0">
                    <Image Source="/NavegadorWeb;component/Icons/home.png" Width="20" Height="20"/>
                </Button>

                <TextBox x:Name="AddressBar" Grid.Column="4"
                         Text="{Binding SelectedTabItem.Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         KeyDown="AddressBar_KeyDown"
                         GotFocus="AddressBar_GotFocus"
                         LostFocus="AddressBar_LostFocus"
                         ToolTip="Enter web address"/>

                <Button Grid.Column="5" Click="NewTabButton_Click" ToolTip="New Tab" Margin="5,0,0,0">
                    <Image Source="/NavegadorWeb;component/Icons/new_tab.png" Width="20" Height="20"/>
                </Button>
                
                <Button Grid.Column="6" Click="HistoryButton_Click" ToolTip="Historial" Margin="5,0,0,0">
                    <Image Source="/NavegadorWeb;component/Icons/history.png" Width="20" Height="20"/>
                </Button>
                
                <Button Grid.Column="7" Click="SettingsButton_Click" ToolTip="Settings" Margin="5,0,0,0">
                    <Image Source="/NavegadorWeb;component/Icons/settings.png" Width="20" Height="20"/>
                </Button>
            </Grid>

            <Border x:Name="FindBar"
                    Grid.Row="2" Grid.Column="0"
                    Background="{StaticResource GeminiBackgroundColor}"
                    BorderThickness="0,1,0,1"
                    BorderBrush="{StaticResource GeminiControlBackgroundBrush}"
                    Visibility="Collapsed"
                    Height="35"
                    Padding="5">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBox x:Name="FindTextBox"
                             Width="200"
                             Margin="0,0,5,0"
                             TextChanged="FindTextBox_TextChanged"
                             KeyDown="FindTextBox_KeyDown"
                             ToolTip="Find on page"/>
                    <Button Content="▲"
                            Click="FindPreviousButton_Click"
                            Margin="0,0,5,0"
                            ToolTip="Find previous"/>
                    <Button Content="▼"
                            Click="FindNextButton_Click"
                            Margin="0,0,5,0"
                            ToolTip="Find next"/>
                    <Button Content="✖"
                            Click="CloseFindBarButton_Click"
                            ToolTip="Close search"/>
                </StackPanel>
            </Border>

            <TabControl x:Name="BrowserTabs"
                        Grid.Row="3" Grid.Column="0"
                        ItemsSource="{Binding TabGroupManager.SelectedTabGroup.TabsInGroup}"
                        SelectedItem="{Binding SelectedTabItem, Mode=TwoWay}"
                        SelectionChanged="BrowserTabControl_SelectionChanged_Grouped"
                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                        ItemContainerStyle="{StaticResource TabItemStyle}"
                        ItemTemplate="{StaticResource TabHeaderTemplate}">
            </TabControl>

            <ProgressBar Grid.Row="4"
                         Height="5"
                         Value="{Binding DownloadProgress}"
                         Visibility="{Binding DownloadProgressBarVisibility}"
                         Background="#333" Foreground="#007bff"
                         BorderThickness="0"
                         Margin="0,0,0,0"
                         VerticalAlignment="Bottom"/>
        </Grid>
    </Border>
</Window>
