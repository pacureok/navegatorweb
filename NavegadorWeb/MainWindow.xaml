<Window x:Class="NavegadorWeb.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NavegadorWeb"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Aurora Browser" Height="768" Width="1024"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        SourceInitialized="MainWindow_SourceInitialized"
        StateChanged="Window_StateChanged">
    <Window.InputBindings>
        <MouseBinding MouseAction="LeftClick" Command="{Binding DragWindowCommand}" />
    </Window.InputBindings>
    <Window.MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Color x:Key="DefaultBrowserBackgroundColor">#282c34</Color>
        <SolidColorBrush x:Key="DefaultBrowserBackgroundBrush" Color="{StaticResource DefaultBrowserBackgroundColor}"/>

        <Color x:Key="DefaultBrowserForegroundColor">#abb2bf</Color>
        <SolidColorBrush x:Key="DefaultBrowserForegroundBrush" Color="{StaticResource DefaultBrowserForegroundColor}"/>

        <Color x:Key="GeminiBackgroundColor">#343a40</Color>
        <SolidColorBrush x:Key="GeminiBackgroundBrush" Color="{StaticResource GeminiBackgroundColor}"/>

        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Header}"/>
                            <Button Content="✖"
                                    Margin="5,0,0,0"
                                    Command="{Binding CloseTabCommand}"
                                    // O si lo manejas en code-behind:
                                    // Click="CloseTabButton_Click"
                                    // Tag="{Binding}" /> </StackPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="TabHeaderTemplate">
            <StackPanel Orientation="Horizontal" ToolTip="{Binding Url}">
                <Image Source="{Binding FaviconSource}" Width="16" Height="16" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Title}" VerticalAlignment="Center" MaxWidth="150" TextTrimming="CharacterEllipsis"/>
                <Button Content="✖"
                        Margin="5,0,0,0"
                        Style="{StaticResource TabCloseButtonStyle}"
                        Click="CloseTabButton_Click"
                        Tag="{Binding}" /> </StackPanel>
        </DataTemplate>

        <Style x:Key="TabCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource DefaultBrowserForegroundColor}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="2,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#555"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#777"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Border x:Name="MainBorder" BorderThickness="1" CornerRadius="8" Background="{StaticResource DefaultBrowserBackgroundBrush}">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#007bff" Offset="0.0"/>
                <GradientStop Color="#6610f2" Offset="0.5"/>
                <GradientStop Color="#fd7e14" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Grid x:Name="MainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>   <RowDefinition Height="40"/>   <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid x:Name="TitleBarGrid" Grid.Row="0" Grid.Column="0" Background="{StaticResource DefaultBrowserBackgroundBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Image x:Name="WindowIcon" Source="/Icons/app.ico" Width="16" Height="16" Margin="5,0,0,0" Grid.Column="0"/>
                <TextBlock Text="{Binding Title}" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{StaticResource DefaultBrowserForegroundColor}"/>

                <Button x:Name="MinimizeButton" Content="—" Grid.Column="2" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" Click="MinimizeButton_Click"/>
                <Button x:Name="MaximizeRestoreButton" Content="◻" Grid.Column="3" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" Click="MaximizeRestoreButton_Click"/>
                <Button x:Name="CloseButton" Content="✕" Grid.Column="4" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" Click="CloseButton_Click"/>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="0" Background="{StaticResource DefaultBrowserBackgroundBrush}" Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="*"/>    <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> <ColumnDefinition Width="Auto"/> </Grid.ColumnDefinitions>

                <Button x:Name="BackButton" Content="◀" Grid.Column="0" Margin="5,0" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" ToolTip="Go back"/>
                <Button x:Name="ForwardButton" Content="▶" Grid.Column="1" Margin="0,0,5,0" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" ToolTip="Go forward"/>
                <Button x:Name="RefreshButton" Content="↻" Grid.Column="2" Margin="0,0,5,0" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" ToolTip="Refresh page"/>

                <Border Grid.Column="3" Background="#3a3e46" CornerRadius="5" Padding="5,0" Margin="0,0,5,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="AddressBar"
                                 Grid.Column="0"
                                 Text="https://www.google.com"
                                 Background="Transparent"
                                 BorderBrush="Transparent"
                                 Foreground="{StaticResource DefaultBrowserForegroundColor}"
                                 VerticalAlignment="Center"
                                 KeyDown="AddressBar_KeyDown"/>
                        <Button x:Name="GeminiButton" Content="AI" Grid.Column="1" ToolTip="Ask Gemini" Click="GeminiButton_Click" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" BorderThickness="0"/>
                        <Button x:Name="PipButton" Content="PiP" Grid.Column="2" ToolTip="Picture-in-Picture" Click="PipButton_Click" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" BorderThickness="0"/>
                        <Button x:Name="ReadAloudButton" Content="🔊" Grid.Column="3" ToolTip="Read Aloud" Click="ReadAloudButton_Click" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" BorderThickness="0"/>
                    </Grid>
                </Border>

                <Button x:Name="NewTabButton" Content="+" Grid.Column="4" Margin="0,0,5,0" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" ToolTip="New tab"/>
                <Button x:Name="FindButton" Content="🔎" Grid.Column="5" Margin="0,0,5,0" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" ToolTip="Find in page" Click="FindButton_Click"/>
                <Button x:Name="SettingsButton" Content="⚙" Grid.Column="6" Width="30" Height="30" Background="Transparent" Foreground="{StaticResource DefaultBrowserForegroundColor}" ToolTip="Settings" Click="SettingsButton_Click"/>
            </Grid>

            <Border x:Name="FindBar" Grid.Row="2" Grid.Column="0"
                    Visibility="Collapsed" Background="#3a3e46"
                    Margin="0,5,0,0" Padding="5">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Find:" VerticalAlignment="Center" Foreground="{StaticResource DefaultBrowserForegroundColor}" Margin="0,0,5,0"/>
                    <TextBox x:Name="FindTextBox" Width="200"
                             Background="#4a4f59" Foreground="{StaticResource DefaultBrowserForegroundColor}"
                             BorderBrush="Transparent" VerticalAlignment="Center"
                             KeyDown="FindTextBox_KeyDown"
                             TextChanged="FindTextBox_TextChanged"/>
                    <Button Content="▲"
                            Click="FindPreviousButton_Click"
                            Margin="5,0,0,0"
                            ToolTip="Find previous"/>
                    <Button Content="▼"
                            Click="FindNextButton_Click"
                            Margin="0,0,5,0"
                            ToolTip="Find next"/>
                    <Button Content="✖"
                            Click="CloseFindBarButton_Click"
                            ToolTip="Close search"/>
                </StackPanel>
            </Border>

            <TabControl x:Name="BrowserTabs"
                        Grid.Row="3" Grid.Column="0"
                        ItemsSource="{Binding TabGroupManager.SelectedTabGroup.TabsInGroup}"
                        SelectedItem="{Binding SelectedTabItem, Mode=TwoWay}"
                        SelectionChanged="BrowserTabControl_SelectionChanged_Grouped"
                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                        ItemContainerStyle="{StaticResource TabItemStyle}"
                        ItemTemplate="{StaticResource TabHeaderTemplate}">
            </TabControl>

            <ProgressBar Grid.Row="4"
                         Height="5"
                         Value="{Binding DownloadProgress}"
                         Visibility="{Binding DownloadProgressBarVisibility}"
                         Background="#333" Foreground="#007bff"
                         BorderThickness="0"
                         Margin="0,0,0,0"
                         VerticalAlignment="Bottom"/>
        </Grid>
    </Border>
</Window>
