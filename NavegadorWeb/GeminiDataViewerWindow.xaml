<Window x:Class="NavegadorWeb.GeminiDataViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NavegadorWeb"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" <!-- ¡ESTA LÍNEA ES LA CLAVE! -->
        mc:Ignorable="d"
        Title="Datos para Gemini" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#F0F0F0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Información Capturada para Gemini" FontSize="20" FontWeight="Bold" Margin="10" HorizontalAlignment="Center" Foreground="#333"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Pregunta del Usuario:" FontWeight="SemiBold" Margin="0,0,5,0" VerticalAlignment="Center" Foreground="#555"/>
            <TextBox x:Name="UserQuestionTextBox" Width="400" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MaxHeight="60" Background="#E0E0E0" BorderBrush="#CCC" Padding="5"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="10">
            <StackPanel x:Name="CapturedDataPanel">
                <!-- Los datos capturados se añadirán aquí dinámicamente desde el código C# -->
                <!-- Ejemplo de cómo se vería un elemento: -->
                <!--
                <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,5,0,5" Background="White">
                    <StackPanel>
                        <TextBlock Text="URL:" FontWeight="Bold"/>
                        <TextBlock Text="https://ejemplo.com/pagina1" TextWrapping="Wrap" Foreground="Blue"/>
                        <TextBlock Text="Título:" FontWeight="Bold" Margin="0,5,0,0"/>
                        <TextBlock Text="Título de la Página de Ejemplo" TextWrapping="Wrap"/>
                        <TextBlock Text="Texto Extraído:" FontWeight="Bold" Margin="0,5,0,0"/>
                        <TextBlock Text="Este es un fragmento del texto extraído de la página..." TextWrapping="Wrap" MaxHeight="100" VerticalScrollBarVisibility="Auto"/>
                        <TextBlock Text="Captura de Pantalla:" FontWeight="Bold" Margin="0,5,0,0"/>
                        <Image Source="data:image/png;base64,..." MaxHeight="200" Stretch="Uniform"/>
                        <TextBlock Text="Favicon:" FontWeight="Bold" Margin="0,5,0,0"/>
                        <Image Source="data:image/x-icon;base64,..." Width="24" Height="24" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>
                -->
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button x:Name="SendToGeminiButton" Content="Enviar a Gemini" Padding="10,5" Margin="0,0,10,0" Background="#9B59B6" Foreground="White" FontWeight="Bold" BorderBrush="#8E44AD" BorderThickness="1" Click="SendToGeminiButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancelar" Padding="10,5" Background="#E74C3C" Foreground="White" FontWeight="Bold" BorderBrush="#C0392B" BorderThickness="1" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
```
**Cambio:** Se añadió `xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"` a la etiqueta `<Window>`.

---

#### **2. `NavegadorWeb\MainWindow.xaml` (Corrección de `MC3072`)**

**Ruta:** `C:\Users\DELL\Downloads\navegatorweb-main\NavegadorWeb\MainWindow.xaml`


```xml
<Window x:Class="NavegadorWeb.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" <!-- Asegúrate de que esta línea esté presente -->
        xmlns:local="clr-namespace:NavegadorWeb"
        mc:Ignorable="d"
        Title="Navegador Web Avanzado" Height="768" Width="1366"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded" Closing="Window_Closing"
        StateChanged="MainWindow_StateChanged">

    <Window.Resources>
        <!-- Convertidor para Visibilidad -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <!-- Estilo para los botones de la barra de título (minimizar, maximizar, cerrar) -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#555555"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para el botón de cerrar de la barra de título (fondo rojo en hover) -->
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#F1707A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para botones de la barra de herramientas (navegación, acciones) -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#555555"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#888888"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para el TextBlock de la barra de estado -->
        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Estilo para las cabeceras de las pestañas -->
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="border" BorderBrush="Transparent" BorderThickness="0" CornerRadius="5,5,0,0" Margin="2,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="contentPresenter"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"
                                                  />
                                <Button x:Name="CloseTabButton" Grid.Column="1" Content="✕"
                                        Style="{StaticResource TitleBarButtonStyle}"
                                        Margin="0,0,5,0" Padding="3" Width="20" Height="20"
                                        HorizontalAlignment="Center" VerticalAlignment="Center"
                                        Visibility="Collapsed"
                                        Click="CloseTabButton_Click"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#282828"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter TargetName="CloseTabButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                                <Setter TargetName="CloseTabButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" ToolTip="{Binding SourceUrl}" FontWeight="Normal" Margin="5,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="10,5,5,5"/>
        </Style>

    </Window.Resources>

    <!-- La propiedad CornerRadius se aplica al Border principal que envuelve el Grid -->
    <Border BorderThickness="1" BorderBrush="#333333" CornerRadius="8" Background="#1A1A1A">
        <Grid x:Name="mainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Barra de Título -->
                <RowDefinition Height="Auto"/> <!-- Barra de Herramientas y Pestañas -->
                <RowDefinition Height="*"/>    <!-- Contenido del Navegador (WebView2) -->
                <RowDefinition Height="Auto"/> <!-- Barra de Estado y Descargas -->
            </Grid.RowDefinitions>

            <!-- Barra de Título Personalizada -->
            <Grid Grid.Row="0" Height="30" Background="#1a1a1a" MouseLeftButtonDown="Window_Drag">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Image Source="/NavegadorWeb;component/Resources/logo.ico" Width="18" Height="18" Margin="5,0,0,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="WindowTitleText" Grid.Column="1" Text="Navegador Web Avanzado" VerticalAlignment="Center" Margin="5,0,0,0" FontWeight="SemiBold" Foreground="White"/>

                <Button x:Name="MinimizeButton" Grid.Column="2" Content="🗕" Style="{StaticResource TitleBarButtonStyle}" Click="MinimizeButton_Click"/>
                <Button x:Name="MaximizeRestoreButton" Grid.Column="3" Content="⬜" Style="{StaticResource TitleBarButtonStyle}" Click="MaximizeRestoreButton_Click"/>
                <Button x:Name="CloseButton" Grid.Column="4" Content="✕" Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click"/>
            </Grid>

            <!-- Barra de Herramientas y Pestañas -->
            <Grid Grid.Row="1" Background="#282828">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Pestañas -->
                    <RowDefinition Height="Auto"/> <!-- Barra de URL y Botones -->
                </Grid.RowDefinitions>

                <!-- Controles de Pestañas -->
                <TabControl x:Name="BrowserTabs" Grid.Row="0" Margin="0,2,0,0" SelectionChanged="BrowserTabs_SelectionChanged"
                            BorderThickness="0" Background="Transparent" Foreground="#FFFFFF">
                    <TabControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" ToolTip="{Binding SourceUrl}">
                                <!-- Icono de candado para HTTPS (se controlará en code-behind) -->
                                <Image x:Name="HttpsIcon" Width="16" Height="16" Margin="0,0,5,0" VerticalAlignment="Center"
                                       Visibility="{Binding IsSecure, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Image.Source>
                                        <BitmapImage UriSource="/NavegadorWeb;component/Resources/lock_icon.png"/>
                                    </Image.Source>
                                </Image>
                                <TextBlock Text="{Binding DisplayTitle}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </TabControl.ItemTemplate>
                </TabControl>


                <!-- Barra de Navegación (URL, Atrás/Adelante, Recargar, etc.) -->
                <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5" VerticalAlignment="Center">
                    <Button x:Name="BackButton" Content="◀" Style="{StaticResource ToolbarButtonStyle}" Click="BackButton_Click" IsEnabled="False" ToolTip="Atrás"/>
                    <Button x:Name="ForwardButton" Content="▶" Style="{StaticResource ToolbarButtonStyle}" Click="ForwardButton_Click" IsEnabled="False" ToolTip="Adelante"/>
                    <Button x:Name="ReloadButton" Content="⟳" Style="{StaticResource ToolbarButtonStyle}" Click="ReloadButton_Click" ToolTip="Recargar"/>
                    <Button x:Name="StopButton" Content="✖" Style="{StaticResource ToolbarButtonStyle}" Click="StopButton_Click" ToolTip="Detener"/>

                    <TextBox x:Name="AddressBar" Text="about:blank" VerticalAlignment="Center" Width="Auto"
                             MinWidth="200"
                             Margin="5,0" Padding="5" FontSize="14"
                             Background="#3A3A3A" Foreground="#E0E0E0" BorderBrush="#555555" BorderThickness="1" CornerRadius="5"
                             KeyUp="AddressBar_KeyUp" ToolTip="Ingresa una URL o término de búsqueda"/>

                    <Button x:Name="GoButton" Content="Ir" Style="{StaticResource ToolbarButtonStyle}" Click="GoButton_Click" ToolTip="Ir a la dirección"/>

                    <!-- Botón para añadir nueva pestaña -->
                    <Button x:Name="NewTabButton" Content="➕" Style="{StaticResource ToolbarButtonStyle}" Click="NewTabButton_Click" ToolTip="Abrir Nueva Pestaña"/>

                    <!-- Nuevo botón para Buscaminas sin conexión -->
                    <Button x:Name="MinesweeperButton" Content="💣" Style="{StaticResource ToolbarButtonStyle}" Click="MinesweeperButton_Click" ToolTip="Jugar Buscaminas (requiere conexión)"/>

                    <!-- Menú de herramientas/opciones -->
                    <Menu Background="Transparent" VerticalAlignment="Center" Margin="5,0,0,0">
                        <MenuItem Header="⋮" Foreground="#E0E0E0" FontSize="18" FontWeight="Bold" Background="Transparent">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="8,5"/>
                                    <Setter Property="Margin" Value="2,0"/>
                                    <Setter Property="Foreground" Value="#E0E0E0"/>
                                    <Setter Property="FontWeight" Value="Normal"/>
                                    <Setter Property="FontSize" Value="16"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="MenuItem">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#333333"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#555555"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>

                            <MenuItem Header="Modo Oscuro" Click="ToggleDarkMode_Click" Background="#3A3A3A" Foreground="#E0E0E0"/>
                            <MenuItem Header="Favoritos" Background="#3A3A3A" Foreground="#E0E0E0">
                                <MenuItem Header="Añadir a Favoritos" Click="AddToFavorites_Click" Background="#3A3A3A" Foreground="#E0E0E0"/>
                                <Separator/>
                                <MenuItem x:Name="FavoritesMenu" Background="#3A3A3A" Foreground="#E0E0E0">
                                    <!-- Los favoritos se añadirán aquí dinámicamente -->
                                </MenuItem>
                            </MenuItem>
                            <MenuItem Header="Zoom" Background="#3A3A3A" Foreground="#E0E0E0">
                                <MenuItem Header="Acercar (Ctrl + +)" Click="ZoomIn_Click" Background="#3A3A3A" Foreground="#E0E0E0"/>
                                <MenuItem Header="Alejar (Ctrl + -)" Click="ZoomOut_Click" Background="#3A3A3A" Foreground="#E0E0E0"/>
                                <MenuItem Header="Zoom al 100%" Click="ZoomReset_Click" Background="#3A3A3A" Foreground="#E0E0E0"/>
                            </MenuItem>
                            <MenuItem Header="Descargas" Click="OpenDownloadsFolder_Click" Background="#3A3A3A" Foreground="#E0E0E0"/>
                            <MenuItem Header="Nueva Pestaña InPrivate" Click="NewInPrivateTab_Click" Background="#3A3A3A" Foreground="#E0E0E0"/>
                        </MenuItem>
                    </Menu>
                </StackPanel>
            </Grid>

            <!-- Contenido del Navegador (Donde se cargan las pestañas) -->
            <Grid Grid.Row="2" Margin="0">
                <!-- El contenido de las pestañas (WebView2) se insertará aquí dinámicamente -->
            </Grid>

            <!-- Barra de Estado e Información de Descargas -->
            <StatusBar Grid.Row="3" Height="25" Background="#1a1a1a" Foreground="#AAAAAA" BorderThickness="0">
                <StatusBarItem>
                    <TextBlock x:Name="StatusBarText" Text="Listo." Style="{StaticResource StatusTextStyle}"/>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <ProgressBar x:Name="DownloadProgressBar" Width="100" Height="15" Margin="0,0,5,0"
                                 Visibility="Collapsed" Background="#555555" Foreground="#007ACC"
                                 BorderBrush="#333333" BorderThickness="1"/>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <TextBlock x:Name="DownloadStatusText" Text="" Style="{StaticResource StatusTextStyle}" Margin="0,0,10,0"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Border>
</Window>
```
**Cambio:** La propiedad `CornerRadius` se aplica ahora al `Border` que envuelve el `Grid` principal de la ventana. Esto es el uso correcto de `CornerRadius` en WPF.

---

### **Pasos Finales para Corregir y Compilar:**

1.  **Cierra Visual Studio completamente.**
2.  **Abre `NavegadorWeb.csproj`** (ruta: `C:\Users\DELL\Downloads\navegatorweb-main\NavegadorWeb\NavegadorWeb.csproj`) con el Bloc de Notas y **reemplaza todo su contenido** con el código que te di en mi respuesta anterior (el que elimina los `<Compile Include="..." />` duplicados). **Guarda y cierra.**
3.  **Abre `GeminiDataViewerWindow.xaml`** (ruta: `C:\Users\DELL\Downloads\navegatorweb-main\NavegadorWeb\GeminiDataViewerWindow.xaml`) con el Bloc de Notas y **reemplaza todo su contenido** con el código que te di en la sección 1 de esta respuesta. **Guarda y cierra.**
4.  **Abre `MainWindow.xaml`** (ruta: `C:\Users\DELL\Downloads\navegatorweb-main\NavegadorWeb\MainWindow.xaml`) con el Bloc de Notas y **reemplaza todo su contenido** con el código que te di en la sección 2 de esta respuesta. **Guarda y cierra.**
5.  **Abre Visual Studio.**
6.  Abre tu solución `NavegadorWeb.sln` (ruta: `C:\Users\DELL\Downloads\navegatorweb-main\NavegadorWeb.sln`).
7.  Una vez que la solución cargue, ve a `Compilar` (Build) > `Recompilar solución` (Rebuild Solution).

Si has seguido todos los pasos correctamente, especialmente la corrección del `.sln` y la reestructuración de carpetas, y ahora estas correcciones en los archivos XAML y `.csproj`, tu proyecto debería compilarse sin errores.

Después de la compilación exitosa, podrás hacer clic en el botón de "Iniciar" (el triángulo verde) para ver tu navegador en funcionamien
